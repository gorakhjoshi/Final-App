const baseUnit = 4;

export const Spacing = {
  u1: baseUnit + "px",
  u2: baseUnit * 2 + "px",
};

export const textSize = [30, 20];

export const FontSize = {
  u1: textSize[0] + "px",
  u2: textSize[1] + "px",
};

export const Colors = "black";
